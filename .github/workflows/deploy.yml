name: Deploy Ubiquiti LLM API to VPS

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup VPS and Deploy
        uses: appleboy/ssh-action@v1.2.0
        env:
          UISP_BASE_URL: ${{ secrets.UISP_BASE_URL }}
          UISP_TOKEN: ${{ secrets.UISP_TOKEN }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          WHATSAPP_API_URL: ${{ secrets.WHATSAPP_API_URL }}
          WHATSAPP_ENABLED: ${{ secrets.WHATSAPP_ENABLED }}
          WHATSAPP_PHONE_COMPLETE: ${{ secrets.WHATSAPP_PHONE_COMPLETE }}
          WHATSAPP_PHONE_SUMMARY: ${{ secrets.WHATSAPP_PHONE_SUMMARY }}
          POLLING_ENABLED: ${{ secrets.POLLING_ENABLED }}
          POLLING_INTERVAL_SECONDS: ${{ secrets.POLLING_INTERVAL_SECONDS }}
          ALERT_OUTAGE_THRESHOLD_PERCENT: ${{ secrets.ALERT_OUTAGE_THRESHOLD_PERCENT }}
        with:
          host: "190.7.234.37"
          username: "root"
          password: "YmUeXJYrO3"
          port: 22
          command_timeout: 30m
          script: |
            echo "🔧 Verificando dependencias..."
            
            # Verificar Docker
            if ! command -v docker &> /dev/null; then
              echo "📦 Instalando Docker..."
              curl -fsSL https://get.docker.com -o get-docker.sh
              sh get-docker.sh
              rm get-docker.sh
            else
              echo "✅ Docker ya está instalado"
            fi
            
            # Instalar Git si no existe
            if ! command -v git &> /dev/null; then
              echo "📦 Instalando Git..."
              apt update && apt install -y git curl
            else
              echo "✅ Git ya está instalado"
            fi
            
            # Verificar versiones
            echo "📋 Versiones instaladas:"
            docker --version
            docker compose version
            git --version
            
            # Configurar directorio del proyecto
            PROJECT_DIR="/opt/ubiquiti-llm"
            
            # Crear directorio si no existe
            mkdir -p $PROJECT_DIR
            cd $PROJECT_DIR
            
            # Si el repo no existe, clonarlo
            if [ ! -d ".git" ]; then
              echo "📦 Clonando repositorio..."
              git clone https://github.com/rhernandezbas/ubiquiti__lmm.git .
            else
              echo "🔄 Actualizando repositorio..."
              git fetch origin
              git reset --hard origin/main
              git pull origin main
            fi
            
            # Generar archivo .env desde GitHub Secrets
            echo "📝 Generando archivo .env desde GitHub Secrets..."
            cat > .env << ENVFILE
            # UISP Configuration
            UISP_BASE_URL=$UISP_BASE_URL
            UISP_TOKEN=$UISP_TOKEN

            # OpenAI Configuration
            OPENAI_API_KEY=$OPENAI_API_KEY
            LLM_MODEL=gpt-4o-mini

            # Database Configuration
            DATABASE_URL=$DATABASE_URL

            # Logging Configuration
            LOG_LEVEL=INFO

            # WhatsApp Service Configuration
            WHATSAPP_API_URL=$WHATSAPP_API_URL
            WHATSAPP_ENABLED=$WHATSAPP_ENABLED
            WHATSAPP_PHONE_COMPLETE=$WHATSAPP_PHONE_COMPLETE
            WHATSAPP_PHONE_SUMMARY=$WHATSAPP_PHONE_SUMMARY

            # Alerting & Polling Configuration
            POLLING_ENABLED=$POLLING_ENABLED
            POLLING_INTERVAL_SECONDS=$POLLING_INTERVAL_SECONDS
            ALERT_OUTAGE_THRESHOLD_PERCENT=$ALERT_OUTAGE_THRESHOLD_PERCENT
            ENVFILE

            echo "✅ Archivo .env generado correctamente"
            
            # Detener contenedores existentes
            echo "🛑 Deteniendo contenedores..."
            docker compose down 2>/dev/null || true
            
            # Limpiar imágenes antiguas
            echo "🧹 Limpiando imágenes antiguas..."
            docker image prune -af
            
            # Construir y levantar con los nuevos cambios
            echo "🏗️  Construyendo nueva imagen..."
            docker compose build --no-cache --pull
            
            echo "🚀 Iniciando contenedores..."
            docker compose up -d
            
            # Esperar a que el contenedor esté listo
            echo "⏳ Esperando que el servicio esté listo..."
            sleep 20
            
            # Mostrar estado
            echo "📊 Estado de contenedores:"
            docker compose ps
            
            # Mostrar logs recientes
            echo "📋 Últimos logs:"
            docker compose logs --tail=30
            
            echo "✅ Despliegue completado exitosamente"

      - name: Verify deployment
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: "190.7.234.37"
          username: "root"
          password: "YmUeXJYrO3"
          port: 22
          script: |
            cd /opt/ubiquiti-llm
            
            echo "🔍 Verificando estado del sistema..."
            
            # Verificar Docker
            if ! command -v docker &> /dev/null; then
              echo "❌ Docker no está instalado"
              exit 1
            fi
            
            echo "✅ Docker disponible: $(docker --version)"
            
            # Verificar si Docker está corriendo (sin sudo)
            if ! docker info &> /dev/null; then
              echo "❌ Docker no está corriendo, intentando iniciar sin sudo..."
              # Intentar iniciar Docker sin sudo
              docker info &> /dev/null || {
                echo "❌ No se puede iniciar Docker sin permisos"
                echo "🔍 Requiere acceso root o configuración de Docker"
                exit 1
              }
            fi
            
            echo "✅ Docker corriendo"
            
            # Verificar contenedores
            echo "📊 Verificando contenedores..."
            docker compose ps -a
            
            # Si no están corriendo, levantarlos
            if ! docker compose ps | grep -q "Up"; then
              echo "🚀 Levantando contenedores..."
              docker compose up --build -d
              sleep 10
            fi
            
            # Verificar que estén corriendo
            if docker compose ps | grep -q "Up"; then
              echo "✅ Contenedores corriendo correctamente"
              echo ""
              echo "📊 Estado de servicios:"
              docker compose ps
              echo ""
              echo "🌐 API disponible en: http://190.7.234.37:7657"
              echo "📖 Documentación: http://190.7.234.37:7657/docs"
              echo "🏥 Health check: http://190.7.234.37:7657/health"
              echo ""
              echo "📋 Logs de la API:"
              docker compose logs --tail=10 api
              echo ""
              echo "🔍 Variables de entorno:"
              docker compose exec api env | grep -E "(ENCRYPTION_KEY|OPENAI_API_KEY)" || echo "Variables no encontradas"
            else
              echo "❌ Error: Contenedores no están corriendo"
              echo ""
              echo "📋 Logs de error:"
              docker compose logs api
              echo ""
              echo "🔍 Intentando diagnóstico:"
              docker compose ps -a
              echo ""
              echo "📊 Estado del sistema:"
              df -h
              free -h
              docker system df
              exit 1
            fi
